def test_function(required_param: str):
    """This is a test function to verify version bumping
    
    Args:
        required_param (str): The parameter to include in the greeting
        
    Returns:
        str: A greeting message including the provided parameter
    """
    return f"Hello, {required_param}!" 